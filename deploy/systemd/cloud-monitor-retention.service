[Unit]
Description=Cloud Monitor metric retention
Wants=docker.service
After=docker.service network-online.target

[Service]
Type=oneshot
WorkingDirectory=/opt/cloud-monitor
EnvironmentFile=/etc/cloud-monitor/cloud-monitor.env
ExecStart=/usr/bin/docker compose --profile jobs run --rm retention-job
